apply plugin: "war"

bootWar {
    mainClassName = "nz.co.atomiclabs.Content2App"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
